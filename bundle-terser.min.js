function operate(n, ...t) {
    function r(n) {
        return Math.sqrt(n)
    }

    return "+" === n || "add" === n ? function (...n) {
        let t = 0;
        return n.forEach((function (n) {
            t += n
        })), t
    }(...t) : "*" === n || "multiply" === n ? function (...n) {
        let t = 1;
        return n.forEach((function (n) {
            t *= n
        })), t
    }(...t) : "root" === n ? 1 === t.length ? r(t[0]) : t.map((function (n) {
        return r(n)
    })) : "odd" === n ? t[0] % 2 == 0 : void 0
}